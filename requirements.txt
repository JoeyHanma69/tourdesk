# TourDesk AI — Full App Dependencies
# Install with: pip install -r requirements.txt

# Web framework
flask==3.0.3
flask-cors==4.0.1

# ML (pinned to match Colab training environment)
transformers==4.40.2
accelerate==0.28.0
torch>=2.0.0

# Data
pandas>=2.0.0
scikit-learn>=1.3.0

# Environment variable loading
python-dotenv==1.0.1

# AI-generated replies (optional — only used when USE_AI_REPLIES=true)
# Install only the one matching your AI_PROVIDER:
ollama>=0.3.0         # local, free  (AI_PROVIDER=ollama)
anthropic>=0.40.0     # Claude, cloud (AI_PROVIDER=anthropic)

# Production server (optional — for deployment)
gunicorn==22.0.0
