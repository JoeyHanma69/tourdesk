# TourDesk AI — Full App Dependencies
# Install with: pip install -r requirements.txt

# Web framework
flask==3.0.3
flask-cors==4.0.1

# ML (pinned to match Colab training environment)
transformers==4.40.2
accelerate==0.28.0
torch>=2.0.0

# Data
pandas>=2.0.0
scikit-learn>=1.3.0

# Environment variable loading
python-dotenv==1.0.1

# Production server (optional — for deployment)
gunicorn==22.0.0
